# AI 評改服務設定指引（約 10 分鐘）

完成以下步驟後，「創作評改」分頁即可使用。全程免費。

---

## 第一步：取得 Google Gemini API 金鑰（免費）

1. 到 [Google AI Studio](https://aistudio.google.com/)
2. 用 Google 帳號登入
3. 點左下角 **「Get API key」** → **「Create API key」**
4. 複製金鑰（一串以 `AIza` 開頭的字符），妥善保存

> 免費額：每日 1,500 次請求，全班使用綽綽有餘。

---

## 第二步：建立 Cloudflare Worker（免費）

1. 到 [Cloudflare](https://dash.cloudflare.com/) 註冊帳號（免費计划即可）
2. 左側選 **「Workers & Pages」** → **「Create」** → **「Create Worker」**
3. 隨意命名（例如 `spectrum-grading`）→ **「Deploy」**
4. 點 **「Edit code」**，把編輯器內的預設程式碼**全部刪除**
5. 打開本資料夾的 **`cloudflare-worker.js`**，**全選複製**，貼到編輯器
6. 點右上角 **「Deploy」**

## 第三步：把 API 金鑰設為密鑰（Secret）

1. 在 Worker 頁面，選 **「Settings」** → **「Variables and Secrets」**
2. 點 **「Add」**，類型選 **「Secret」**
3. 名稱輸入：`GEMINI_API_KEY`
4. 值貼上：第一步取得的 `AIza...` 金鑰
5. 點 **「Deploy」** 儲存

## 第四步：取得 Worker 網址

1. 在 Worker 的 **「Settings」** → **「Domains & Routes」**，會看到預設網址，格式如：
   `https://spectrum-grading.你的帳號.workers.dev`
2. **把這個網址交給我**，我會嵌入網頁並完成部署

（如果你懂編輯，也可以自行打開 `index.html`，搜尋 `WORKER_URL`，把網址貼在引號內，commit 並 push 即可。）

---

## 常見問題

| 問題 | 解答 |
|------|------|
| 學生會看到我的金鑰嗎？ | 不會。金鑰只存在 Cloudflare 的密鑰庫，網頁只見 Worker 網址。 |
| 別人可以用我的 Worker 嗎？ | Worker 已限制只接受來自 `emilmich.github.io` 的請求。 |
| 超出免費額怎麼辦？ | Gemini 每日 1,500 次、Worker 每日 100,000 次；一班約用 300 次。如真的超額，當日稍後會暫停，翌日自動恢復。 |
| 學生作品會被儲存嗎？ | 不會。作品只在瀏覽器與 API 之間傳輸，Worker 不儲存任何資料。 |
